"name":"unpack-src","on":{"workflow_dispatch":{}},"permissions":{"contents":"write"},"jobs":{"unpack":{"runs-on":"ubuntu-latest","steps":[{"uses":"actions/checkout@v4"},{"run":"unzip -o ik-house-src.zip && rm -f ik-house-src.zip && git config user.name github-actions && git config user.email github-actions@github.com && git add -A && git commit -m Import-IK-HOUSE-source && git push"}]}}}
